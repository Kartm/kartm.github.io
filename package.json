{
  "name": "my-app",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "gh-pages-deploy": "git checkout -f master && git checkout --orphan gh-pages && git --work-tree build add --all && git --work-tree build commit -m 'gh-pages' && git push origin HEAD:gh-pages --force && git checkout -f master",
    "deploy": "next build && next export && cp CNAME ./out/CNAME && npm run gh-pages-deploy",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "12.0.7",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@types/node": "17.0.5",
    "@types/react": "17.0.38",
    "eslint": "8.5.0",
    "eslint-config-next": "12.0.7",
    "typescript": "4.5.4"
  }
}
